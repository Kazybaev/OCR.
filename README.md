work2

# Multilingual OCR Comparator Pro

Сравнение трёх OCR-движков с консенсус-подсветкой слов, детальными таймингами и визуализацией различий.

## Системные требования

- **ОС:** Windows
- **Python:** 3.10
- **Tesseract OCR:** обязательна установка отдельно

## Что делает

- Распознаёт текст тремя OCR-движками одновременно
- **Консенсус-подсветка слов**: зелёный (все 3), жёлтый (2 из 3), красный (только 1)
- **Детальные тайминги**: загрузка и обработка каждой модели отдельно
- **Статистика точности** с графиками и тепловой картой
- Извлечение и сравнение таблиц
- Экспорт результатов (TXT, CSV, Excel)

## Технологии

**OCR-движки:**
- **PaddleOCR** — объединённый (EN + CH модели)
- **Tesseract** — триязычный (eng+rus+chi_sim)
- **EasyOCR** — объединённый (RU + CH модели)

**Таблицы:**
- **PPStructure** — детекция структуры
- **Все три движка** — извлечение текста из ячеек с выбором лучшего результата

**Визуализация:**
- **Matplotlib + Seaborn** — графики точности и тепловые карты
- **SequenceMatcher** — вычисление сходства текстов

**Интерфейс:**
- **Streamlit** — веб-интерфейс с вертикальным отображением

## Новые возможности

### 1. Консенсус-подсветка слов (вертикальное отображение)
**Зачем:** Быстрая визуальная оценка надёжности распознавания каждого слова.
- **Зелёный фон** — слово распознали все 3 движка (высокая уверенность)
- **Жёлтый фон** — слово распознали 2 из 3 (средняя уверенность)
- **Красный фон** — слово распознал только 1 движок (низкая уверенность, возможна ошибка)
- Вертикальное расположение — проще сравнивать длинные тексты

**Проблема:** В горизонтальном посимвольном сравнении сложно быстро понять, насколько надёжен каждый фрагмент текста.

### 2. Детальные тайминги загрузки и обработки
**Зачем:** Понимание узких мест производительности для оптимизации.
- **Загрузка (load)**: сколько времени занимает инициализация каждой модели
  - PaddleOCR EN, PaddleOCR CH, PPStructure, EasyOCR RU, EasyOCR CH
- **Обработка (process)**: сколько времени занимает распознавание конкретного изображения
  - PaddleOCR (EN+CH), Tesseract, EasyOCR (RU+CH)
- Отображение в скрываемом меню (expander)

**Проблема:** Неясно, какой движок работает медленно — сама модель или её загрузка.

### 3. Отображение таймингов сразу после загрузки моделей
**Зачем:** Информирование пользователя о прогрессе при первом запуске.
- Показывает время загрузки каждой модели до загрузки изображения
- Помогает понять, сколько займёт холодный старт приложения

**Проблема:** При первом запуске пользователь не понимает, почему долго загружается приложение.

### 4. Защита от ошибок при распознавании таблиц
**Зачем:** Предотвращение краха приложения при проблемах с PPStructure.
- Попытка обработки как ndarray → при ошибке попытка по пути → при повторной ошибке пропуск таблиц
- Предупреждение пользователя о проблеме вместо краха

**Проблема:** PPStructure иногда падает на некоторых изображениях, прерывая всю обработку.

### 5. Гарантированный показ всех 3 движков
**Зачем:** Полная прозрачность результатов даже при пустом выводе.
- Всегда отображаются PaddleOCR, Tesseract, EasyOCR (даже если результат пустой)
- Пользователь видит, что движок работал, но не нашёл текста

**Проблема:** Если движок ничего не распознал, он исчезал из интерфейса — непонятно, работал ли он вообще.

### 6. Измерение времени с защитой от исключений
**Зачем:** Корректные тайминги даже при ошибках внутри блока обработки.
- `try-finally` обеспечивает запись времени независимо от успеха операции
- Суммирование времени для EasyOCR RU+CH

**Проблема:** При исключении внутри блока тайминги не записывались.

### 7. Использование `use_container_width=True` для изображений
**Зачем:** Адаптивное отображение изображений на любых экранах.
- Изображение масштабируется под ширину контейнера
- Удобный просмотр на мобильных устройствах и мониторах

**Проблема:** Фиксированная ширина делала изображения слишком маленькими или слишком большими.

## Установка

### 1. Установка Tesseract OCR

**Windows:** 
- Скачайте установщик: [Tesseract at UB Mannheim](https://github.com/UB-Mannheim/tesseract/wiki)
- При установке выберите языки: **English**, **Russian**, **Chinese - Simplified**
- Стандартный путь установки: `C:\Program Files\Tesseract-OCR\tesseract.exe`

### 2. Установка Python-зависимостей
```bash
pip install -r requirements.txt
```

### requirements.txt
```text
streamlit==1.51.0
paddleocr==2.6.1.3
paddlepaddle==3.0.0b1
easyocr==1.7.1
opencv-python==4.6.0.66
opencv-contrib-python==4.6.0.66
pandas==2.3.3
pytesseract==0.3.13
numpy==1.26.4
pillow==12.0.0
torch==2.5.1
torchvision==0.20.1
matplotlib==3.10.7
seaborn==0.13.2
Shapely==1.8.5.post1
pyclipper==1.3.0.post6
lmdb==1.7.5
tqdm==4.67.1
imgaug==0.4.0
scikit-image==0.25.2
scipy==1.15.3
PyYAML==6.0.3
requests==2.32.5
python-docx==1.2.0
openpyxl==3.1.5
lxml==6.0.2
Cython==3.2.0
```

### 3. Настройка пути к Tesseract

Откройте `main.py` и укажите путь к Tesseract (строка 19):
```python
pytesseract.pytesseract.tesseract_cmd = r"C:\Program Files\Tesseract-OCR\tesseract.exe"
```

## Запуск
```bash
streamlit run main.py
```

Откроется браузер → загрузите изображение → нажмите "Распознать и сравнить".

## Как работает

1. **Загрузка моделей**: отображение таймингов инициализации
2. **Распознавание**: три движка обрабатывают изображение параллельно
3. **Объединение**: результаты по языкам сливаются в 3 финальных текста
4. **Консенсус-анализ**: подсчёт частоты каждого слова по движкам
5. **Визуализация**: вертикальное отображение с цветовой подсветкой консенсуса
6. **Статистика**: вычисление процентов совпадений, построение графиков
7. **Таблицы**: детекция структуры → извлечение ячеек → выбор лучшего текста
8. **Экспорт**: скачивание всех результатов раздельно

## Интерфейс

- **Вертикальное сравнение**: 3 блока друг под другом
- **Консенсус-подсветка**: 
  - 🟢 Зелёный — все 3 движка согласны
  - 🟡 Жёлтый — 2 из 3 согласны
  - 🔴 Красный — только 1 движок распознал
- **Скрываемые секции**: 
  - Тайминги загрузки/обработки
  - Графики точности и тепловые карты
- **Прогресс-бары**: отображение обработки таблиц

## Особенности

- Первый запуск: модели EasyOCR загружаются автоматически (~100 MB)
- Кэширование моделей для быстрой повторной загрузки
- Работает на CPU (GPU не требуется)
- Детальные тайминги: load vs process для каждой модели
- Graceful degradation: при недоступности движка работает с оставшимися
- Защита от краха при проблемах с таблицами
- Адаптивное отображение изображений

## Производительность

- Предобработка изображений: адаптивная бинаризация + шумоподавление
- Параллельная работа OCR-движков
- Оптимизированная обработка ячеек таблиц с прогресс-баром
- Умная дедупликация без потери уникальных данных
- Измерение времени с защитой от исключений
